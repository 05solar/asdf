# 프로젝트 진행 상황

최종 점검일: 2026-08-06

## 1. 프로젝트 개요

`한글과 세종(HANGUL & SEJONG)`의 카드 소개·제작 신청·발급 조회·고객지원·관리 기능을 제공하는 반응형 웹 프론트엔드다. 현재 저장소는 React와 TypeScript로 작성된 단일 Vite 애플리케이션이며 Netlify 정적 배포를 기준으로 구성되어 있다.

## 2. 현재 기술 구성

- React 18, TypeScript 5.6, Vite 5
- React Router 6 기반 SPA
- CSS 파일과 CSS Custom Properties 기반 디자인 시스템
- API 통신: `src/services/api.ts`
- 배포: `netlify.toml`, `public/_redirects`
- Node.js 배포 기준: 20
- 테스트·린트 도구는 아직 구성되어 있지 않다.

## 3. 구현 현황

| 영역 | 상태 | 비고 |
| --- | --- | --- |
| 공통 헤더·푸터·반응형 레이아웃 | 구현 | 공개 페이지 공통 레이아웃 사용 |
| 메인 페이지 | 구현 | 히어로, 주요 디자인, 서비스 핵심, 기념품, 문의, 협력기관 |
| 디자인 갤러리 | 구현 | 카드 유형별 캐러셀과 신청 화면 연결 |
| 제작 신청 | 구현 | 5단계 입력, 미리보기, 파일 첨부, 완료 화면 |
| 신청 조회·모바일 카드 | 부분 연동 | API 우선 호출 후 데모 데이터로 대체 가능 |
| 로그인·회원가입·계정 찾기 | 부분 연동 | OAuth/API 진입점과 로컬 데모 인증이 함께 존재 |
| 마이페이지 | 부분 연동 | API 프로필과 로컬 활동 데이터 혼용 |
| 후기 | 구현(로컬 데이터) | 작성자명·신청 유형·카드 종류·사진, 검색·필터, 3×3 카드 목록·페이지네이션 |
| 문의·공지·FAQ·행사 | 구현(로컬 데이터) | 브라우저 `localStorage` 기반 CRUD/관리 |
| 관리자 화면 | 데모 구현 | 프론트엔드 권한 가드와 로컬 신청·문의 데이터 사용 |
| 회사 소개·인사말·고객지원 | 구현 | 일부 회사 정보와 콘텐츠는 확정 필요 |
| 다국어 상태 | 기반만 구현 | 언어 선택 상태는 있으나 전체 번역 체계는 미완성 |
| Netlify 배포 설정 | 구현 | `npm run build`, `dist`, SPA fallback 설정 |

현재 등록된 주요 경로는 `/`, `/design`, `/apply/*`, `/lookup`, `/mobile-card`, `/support`, `/inquiry`, `/faq`, `/notices`, `/mypage`, `/login`, `/signup`, `/account-recovery`, `/company`, `/greetings`, `/reviews`, `/events`, `/admin`이다.

## 4. 데이터와 백엔드 연동 상태

`VITE_API_BASE_URL`이 API 기준 URL이다. 요청은 쿠키를 포함하며, 401 응답 시 `/api/auth/refresh`를 한 번 호출한 뒤 원래 요청을 재시도한다.

준비된 API 기능:

- 현재 사용자 조회, 프로필 수정, 회원 탈퇴
- 약관 동의, 토큰 갱신, 로그아웃
- 개인·단체 제작 신청
- 신청 조회
- 반려 사진 재업로드
- 카드 다운로드 정보 조회
- Google·Naver OAuth URL

현재 실제 서버 데이터가 아닌 기능:

- 데모 사용자·관리자 로그인
- 신청·후기·문의·공지·FAQ·행사 관리 데이터
- 관리자 권한 검증(화면 전환 수준이며 서버 보안이 아님)
- 일부 조회 성공 시나리오

후기 첨부 사진은 현재 서버 업로드가 아니라 2MB 이하 Data URL로 `localStorage`에 저장된다. 운영 전에는 이미지 스토리지 업로드 방식으로 교체해야 한다.

개인정보가 포함된 신청 초안은 `sessionStorage`에 저장되며, 첨부한 실제 `File` 객체는 저장하지 않는다. 새로고침 후 제출하려면 파일을 다시 선택해야 할 수 있다.

메인 페이지의 첫 번째 한옥 창문에는 `src/data/nameResults.json`의 이름·한자·영문·뜻풀이 700건이 한 줄씩 이어지는 순환 애니메이션으로 표시된다. 이 데이터는 외부 엑셀 원본에서 화면용으로 변환한 정적 스냅샷이므로 원본 변경 시 다시 동기화해야 한다.

## 5. 확인된 품질 상태

2026-08-06 기준 `npm run build`가 성공했다.

- TypeScript 검사 성공
- Vite 프로덕션 번들 생성 성공
- 130개 모듈 변환
- 산출물: JS 약 301 kB, CSS 약 95 kB(압축 전)

자동화 테스트와 린트 스크립트가 없으므로 현재 빌드 성공만으로 화면 동작 전체를 보장하지는 않는다.

## 6. 다음 우선순위

1. 실제 백엔드 연결 후 로컬 데모 fallback 제거 또는 개발 모드로 격리
2. 서버 측 인증·관리자 권한·입력 검증 확인
3. 회사 정보, 계좌 정보, 특허번호, 연락처 등 임시값 확정
4. 카드 페이지 2~5의 실제 변형 이미지와 최종 브랜드 이미지 반영
5. 업로드 파일 형식·용량·오류 응답 UX 확정
6. 전체 경로 모바일·태블릿·데스크톱 수동 회귀 테스트
7. Vitest/React Testing Library 및 린트 도입
8. 접근성 점검(키보드, 포커스, 오류 안내, 대비, 대체 텍스트)
9. README와 배포 문서의 한글 인코딩·표현 정리

## 7. 배포 전 체크리스트

- [ ] `VITE_API_BASE_URL` 설정
- [ ] `VITE_CARD_TYPE_*` ID를 서버 값과 일치시킴
- [ ] 백엔드 CORS에 Netlify 도메인과 credentials 허용
- [ ] OAuth redirect URI에 운영 도메인 등록
- [ ] 모든 회사·계좌·정책 문구 검수
- [ ] 실제 사용자·관리자 권한으로 주요 흐름 검증
- [ ] 업로드 및 카드 다운로드 만료 URL 검증
- [ ] `npm run build` 재실행
- [ ] Netlify deploy preview에서 SPA 새로고침 확인
