# 진행 상황 (progress.md)

한글과 세종 프론트엔드 데모의 작업 진행 기록입니다. 상세한 구현 범위/미완성 항목은
`PROJECT_STATUS.md`, 시각 규칙은 `DESIGN.md`·`src/styles/tokens.css`, 작업 규칙은
`AGENTS.md`를 함께 참고하세요.

## 최근 작업 (2026-08)

### 카드·콘텐츠 페이지
- 메인 카드 자동 전환과 앞·뒷면 전환, 십이지 선택에 따른 주요 디자인 연동을 구현.
- 디자인 페이지 카드 순서를 실제 가로·세로 카드 자산 기준으로 재구성하고 곡률을 통일.
- 제작 신청 화면에 카드 유형별 견본품을 연결하고 앞·뒷면을 세로 배치.
- 후기 목록을 사진 상단·본문 하단 구조로 변경하고 사진 없는 텍스트 카드 및 본인 후기 수정을 지원.
- 회사 소개에 약도를 추가하고 행사사업에 부스 운영 사진 4장과 브랜드 협업 사진 8장을 추가.
- 사용하지 않는 구형 카드 이미지 36장과 중복 세종 이미지 1장을 제거.

### 제작 신청 플로우 (`src/components/apply`, `src/features/apply`)
- 신청인/수령인 입력과 업로드 항목을 **카드 종류**가 아닌 **신청 유형(개인·법인/단체)**
  기준으로 분기하도록 재구성.
  - 개인: 사주 정보(영문 이름·국적·출생지역·생년월일·출생시간·성별·**한국입국일**·전화번호·이메일).
  - 법인/단체: 이름·법인·단체명(필수)·부서(선택)·연락처·이메일. 수령인에도 법인·단체명/부서(선택) 추가.
  - 학생증: 개인은 위 개인 항목 + 학번·학과, 법인/단체는 `법인·단체명` 대신 `학교명` 사용.
- 사진/파일 등록도 유형별 분기: 개인=프로필 사진(학생은 +학교 로고·직인),
  법인/단체=로고·직인·첨부파일(ZIP).
- 신청 유형 선택 단계에 **문화체험용 기념 증서 안내 동의 체크박스** 추가(사전 상담 동의 아래).
- 최종 확인 단계: 랜덤 배정되는 "디자인" 항목 노출 제거, 박스 외곽선/배경을 없애고
  항목 사이 **구분선**만 사용, `수정` 버튼은 텍스트만 남기고 굵게 표시.
- 모바일 발급만 선택 시 최종 확인에서 수령인 정보 미노출.
- 신청 완료 안내에 개인 신청 한정 입금 기한 문구 유지.
- 신규 필드는 `ApplicantInfo.koreaEntryDate`, `ApplicationDraft.disclaimerConfirmed`.

### 홈/공통 UI
- 메인 첫 번째 한옥 창(이름 풀이): 이름 목록이 **위로 올라가는** 방향으로 애니메이션 변경.
- 회사 소개 지도 하단 `길찾기` 버튼: 호버 시 네이비(`--color-primary`)로 채워지도록 변경
  (메인 신청 버튼과 동일한 효과).
- 신청 조회: **신청번호 없이 전화번호·이메일만으로 조회** 가능하도록 변경.
- 공지사항 제목 앞 마커 색상을 네이비(`--color-primary`)로 변경. 마이페이지 제작 내역
  제목 마커는 기존부터 네이비 유지.
- 마이페이지: `회원정보 수정` 버튼 상시 노출, 우측 상단 히어로 이미지(`support-bg.png`) 추가.
- 디자인 페이지 학생증 1열 두 카드 사이 간격 확대.

## 2026-08-13 작업 (클라이언트 카톡·통화 반영)

### 체크박스 통일
- 공통 체크박스(`.check input`)를 네이티브 `accent-color` 대신 **커스텀(appearance:none)**으로
  변경 → **네이비 사각 + 아이보리 체크**로 모든 브라우저에서 동일. (`src/styles/forms.css`)
- 제작 신청 2단계 "발급 유형 선택"(모바일/모바일+실물) 라디오도 동일한 네이비 사각 외형으로
  (단일 선택 동작 유지). (`StepInfo.tsx`)
- 후기 작성의 "신청자 유형 / 신청한 카드" 선택도 동일 체크박스 디자인 적용 + 각 항목의
  **테두리 박스 제거**. (`ReviewEditorPage.tsx`, `.css`)

### "신청 내역 확인하기" → 마이페이지 제작내역
- 제작 신청 완료 화면 버튼을 `/lookup`(조회) → **`/mypage#production`**(마이페이지 제작내역)로 변경.
- 마이페이지 제작내역 섹션에 `id="production"` 앵커 추가, 제작내역 헤더의 "신청 조회" 링크 제거.
  (`StepComplete.tsx`, `MyPage.tsx`)

### 조회 페이지 재구성 (본인 모바일 카드 확인 전용)
- 회원가입 없이 전화+이메일 또는 카드번호 입력 → **같은 페이지에 카드 인라인 표시 → 클릭 시
  앞/뒷면 플립**. (`LookupPage.tsx`)
- **테스트 훅**: 카드번호 `admin-test` 입력 시 데모 카드 표시(실서비스는 API 결과로 대체).
- 앞면 파일명 `-front`→`-back`로 매칭 뒷면 유도.
- 재사용 `FlipCard` 컴포넌트 신설(`src/components/ui/FlipCard.tsx`, `.css`).
- **플립 backface 버그 수정**: `__inner`의 `filter`가 `preserve-3d`를 flat으로 만들어
  뒷면 대신 앞면이 좌우반전되던 문제 → filter 제거로 해결(FlipCard·MobileCardPage 공통).

### 서브페이지 타이틀 구분선 통일
- `.subpage-hero`에 공통 구분선(전체폭 얇은 선 + 좌측에서 살짝 들어온 **네이비 강조 막대**)을
  적용해 전 페이지 위치·디자인 통일. (`globals.css`)
- 페이지별로 제각각이던 구분선 제거(회사소개/인사말/문의/후기작성 hero border, 디자인
  lead-line, support-rule 헤더, notice-detail rule).
- **제작 신청·조회 페이지는 구분선 숨김.** 네이비 강조 위치는 우측으로 이동.

### 회사소개·인사말 스크롤 등장 효과
- `useScrollReveal` 훅(IntersectionObserver) + `.reveal`/`.reveal-group`/`.reveal-item`.
- 회사소개/인사말 섹션이 스크롤 시 페이드+슬라이드업, 카드·문단은 **stagger(순차)** 등장.
- 요청에 따라 OS "동작 줄이기" 설정과 **무관하게 항상 재생**.

### 행사사업 페이지 전면 재구성 (`EventsPage.tsx`, `.css`)
- 기존 설명형 섹션 제거 → "행사에 맞춰 다양하게 구성합니다 → 행사 진행 과정" 인접.
- "행사 진행 과정"에서 **"프로그램 구성" 제거**, 박스를 항목 수 기준 **균등 배열**.
- 행사 진행 과정 아래 **부스 운영 / 법인·단체 협업** 섹션 신설(공통 `EventFeed`).
  - 타이틀 행: 좌측 **강조 마크**(--color-accent-bar) + **긴 구분선** + 우측 문구
    ("현장에서 고객과 직접 만나 정성을 담은 서비스를 제공합니다").
  - **대표 카드**: 사진 왼쪽 60% / 글 오른쪽 40%, 글 세로 가운데, 자세히 보기 우측 하단.
  - **하단 3카드**: 사진 위/내용 아래, 외곽선 없이 배경색만(#F2E9DF), 자세히 보기 우측 하단.
  - 카드 색상: 타이틀 #5D4533, 날짜 #BC996E(타이틀 아래), 내용 #4D453E, 자세히 보기 #90867D.
    카드에서 위치(장소) 표기 삭제.
  - **"자세히 보기" 팝업**: 좌측 이미지 갤러리(메인+썸네일/‹›), 우측 라벨·제목·날짜·표
    (장소/주최/발급 카드)·본문, ✕ 우상단. 배경 #F2E9DF(사진·글 영역 동일), 표 구분선 제거.
    (`Modal` 재사용, `EventDetail` 컴포넌트)
  - 기존 booth/collaboration 이미지 매핑, 게시글별 `주최(host)`·갤러리 풀 추가.
  - 카드 라벨(명예한국인증·방문증·학생증 등) 표기 반영.

## 2026-08-14 작업

### 페이지 파일 구조 개편
- `src/pages`를 **페이지별 폴더**로 재구성: `src/pages/PageName/PageName.tsx`(+ 같은 폴더의
  `PageName.css`). 기존 `src/pages/apply/`도 `src/pages/ApplyPage/`(`ApplyPage.tsx`·`ApplyPage.css`)로 통일.
- 공용 CSS 정리: 소유 페이지가 없는 `ContentPages.css`는 `src/styles/`로 이동(Events·Greetings·
  Reviews가 공유). 소유 페이지명이 분명한 `LoginPage.css`·`SupportPage.css`·`NoticeDetailPage.css`는
  해당 폴더에 두고 다른 페이지에서 교차 참조.
- `App.tsx`와 페이지 간 import 경로 전면 갱신. 컴포넌트(`src/components`)는 기존 구조 유지.

### 카드 앞면 이미지 교체(신규 시안 PNG)
- 앞면을 새 PNG로 교체(뒷면은 기존 유지): `student-horse`, `kor-tiger`, `kor-mouse`, `visit-mouse`.
  홈 히어로/주요 디자인, 디자인 갤러리, 신청 견본, 리뷰 예시 등 사용처 전부 반영.
- **학생증 4페이지 신설**: `stu-monkey` 가로 카드를 추가해 학생증 목록이 4페이지가 되며,
  4페이지는 **가로 1열(앞·뒷면)** 만 노출.

### 모바일 카드 조회 이미지 다운로드 (`src/lib/cardDownload.ts`)
- 조회(`/lookup`) 결과 화면에 **다운로드 3종** 추가: 앞·뒷면을 브랜드 배경/타이틀과 함께
  canvas로 합성한 **한 장 PNG**, 그리고 **앞면·뒷면 원본 개별 저장**.
- "카드 이미지 다운로드" 버튼은 **메인 신청 버튼(`.design-chip`)과 동일한 디자인**으로 통일.
- 조회 페이지 타이틀 아래 **공통 구분선**을 다른 서브페이지와 동일하게 표시(이전엔 숨김 처리했음).

### 문의 상세 페이지 신설
- 라우트 `/mypage/inquiry/:inquiryId`(`InquiryDetailPage`). 마이페이지 문의 내역 **제목 클릭 시 이동**.
  문의 분류·상태·작성일·내용과 **관리자 답변**을 확인. 본인(또는 관리자)만 열람 가능.
- `InquiryRecord`에 `answer`·`answeredAt` 필드 추가. 관리자 화면 문의 펼침 영역에
  **답변 입력창 + 저장** 추가(저장 시 상태를 자동으로 "문의 완료"로 변경).

## 미완료 / 남은 요구사항 (클라이언트 요청 중 미반영)

> 대부분 **새 카드 이미지 파일 전달**이 선행되어야 하는 항목입니다.

1. **카드 캐릭터/이미지 교체** (예전 시안 → 새 시안).
   - (2026-08-14 반영) 전달받은 신규 PNG로 앞면 교체: `kor-tiger`, `kor-mouse`, `visit-mouse`,
     `student-horse`. 홈·디자인·신청·리뷰 사용처 포함.
   - 남은 항목: 나머지 캐릭터 새 시안 전달 시 동일 방식으로 앞면 교체(뒷면 시안 전달 여부도 확인).
2. **학생증** 관련. *새 이미지/원본 필요.*
   - (2026-08-14 반영) 학생증 4페이지에 원숭이 가로 카드(`stu-monkey`) 추가.
   - 학생증 전부 **하이픈( - ) 제거 버전**으로 교체(예: 학번 2024-xxxx → 하이픈 없이).
   - 학생증 이미지 **화질 저하** → 원본 재전달 필요.
3. **상단바(헤더 네비게이션) 디자인 통일** — 페이지마다 상단바가 다르다는 지적.
   (헤더는 공통 컴포넌트라 로그인/비로그인·스크롤 상태 차이일 가능성 → 확인 후 통일.)
   ※ 이번에 한 것은 "타이틀 밑 구분선" 통일이며, 헤더 자체 통일은 별개.
4. **후기 작성 시 작성자 이름 안내** (문구/예시 미반영).
   - 입력란 placeholder에 예시(예: "홍길동", "문화행사 운영팀"처럼 실명 아니어도 됨).
   - **"익명 처리되지 않습니다"** 안내 문구 표기.
   (현재 placeholder는 "후기에 표시할 이름을 입력해 주세요"만 있음.)
5. **후기 카드 라벨 표기**("명예 시민증 – 학생증" 메모) — 후기 목록/카드의 카드 종류 라벨
   표기 방식 확인·반영 필요. (행사 카드엔 반영했으나 후기 페이지는 미확인.)
6. **행사 페이지 실제 콘텐츠** — 부스운영/협업 게시글의 행사명·일자·장소·주최·사진은 현재
   **예시(자리표시)**. 실제 데이터·사진 세트로 교체 필요. 용어 "페럿카드" 의미 확인 필요.

※ 카드 제작 **프로그램**은 홈페이지와 별개(해원 담당) — 본 저장소 범위 밖.

## 알려진 제한 / 후속 확인 필요
- 신청 폼에 필수값 검증(미입력 차단)은 아직 없음.
- 개인 신청에는 한글 이름 입력란이 없음(요청 반영). 필요 시 추가.
- 실제 API 연동은 `user.source === "api"` 경로에서만 동작하며, 나머지는 로컬 데모 데이터 사용.
- 마이페이지 히어로 이미지 크기/위치는 실제 화면에서 미세 조정 필요할 수 있음.

## 검증
- 2026-08-11 `npm run build` (tsc + vite) 통과.
- 2026-08-13 `npm run build` (tsc + vite) 통과 (이날 작업 전부 반영).
- 2026-08-14 `npm run build` (tsc + vite) 통과 (구조 개편·카드 교체·다운로드·문의 상세 반영).
- 브라우저 실동작 확인은 카드 종류 × 개인/법인·단체 조합별로 별도 필요.
- 2026-08-13 추가분 시각 확인 필요: 조회(admin-test 플립), 서브페이지 구분선, 회사소개
  스크롤 등장, 행사 부스운영/협업 카드·팝업, 후기 작성 체크박스.
- 2026-08-14 추가분 실동작 확인 필요(브라우저): 조회 결과의 이미지 다운로드 3종(합성 PNG·
  앞면·뒷면), 학생증 4페이지 표시, 관리자 답변 저장 → 마이페이지에서 문의 상세·답변 확인.
